<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/reminderFragment">

    <fragment
        android:id="@+id/reminderFragment"
        android:name="com.example.locationreminder.ReminderFragment"
        android:label="reminder_fragment"
        tools:layout="@layout/reminder_fragment" >
        <action
            android:id="@+id/action_reminderFragment_to_saveFragment"
            app:destination="@id/saveFragment" />
    </fragment>
    <fragment
        android:id="@+id/saveFragment"
        android:name="com.example.locationreminder.SaveFragment"
        android:label="save_fragment"
        tools:layout="@layout/save_fragment" />
</navigation>